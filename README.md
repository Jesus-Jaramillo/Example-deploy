#  Aplication description

It's an application that uses ReactJS and Vite as its base. It utilizes a web API that displays images of different types of coffee, along with their name, price, rating, popularity, and availability. 
It also includes a modal where you can view the details of the selected product.
